import React from "react";
import { Link } from "react-router-dom";

const Reset = () => {
  return (
    <div>
      <p>Reset</p>
      <Link to="/">login</Link>
    </div>
  );
};

export default Reset;
